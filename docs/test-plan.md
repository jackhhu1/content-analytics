# Updated Test Plan for the Content Analytics Project
content = """# Test Plan: Viral Intelligence Feed (v2 - Lean Signal)

Generated by Gemini (Test Refinement) on 2026-04-12
Branch: main
Repo: jackhhu1/content-analytics
Status: UPDATED (Metadata focus)

## Affected Routes / Modules

- `/api/webhook/apify` — Metadata ingestion (POST)
- `/api/feed` — Viral Coefficient leaderboard (GET)
- `lib/metrics.ts` — Pure functions: calculateVC, getAccountMedian, detectOutliers
- `/setup` — Niche account management
- `/playbook` — Saved links and user notes

## Unit Tests (Vitest)

### lib/metrics.ts
- `calculateVC(views, followers)`:
    - Normal: 100,000 views / 10,000 followers = 10.0
    - Edge Case: followers = 0 (Return 0, prevent division by zero)
    - Edge Case: views = null (Treat as 0)
- `getAccountMedian(posts[])`:
    - Ensure correct median calculation (not mean) to prevent skewed results from a single viral hit.
    - Handle empty arrays or < 3 posts gracefully.
- `detectOutliers(posts[], threshold=2.0)`:
    - Correctly marks posts where `post.vc >= (median * threshold)`.
    - Returns top outliers sorted by multiplier.

### app/api/webhook/apify/route.ts
- Verify that `follower_count` is correctly captured and stored per-scrape.
- Ensure `is_outlier` boolean is calculated and set on INSERT.
- Check that duplicate `post_id` for the same scrape run are ignored.

## Integration Tests (Supabase)

### RLS & Multi-Tenancy (CRITICAL)
1. **User Isolation:** Ensure User A cannot see the `niche_accounts` or `posts` of User B.
2. **Playbook Privacy:** User B should not be able to read or edit `user_notes` from User A’s playbook entries.

### Database Constraints
- Verify that `posts` table rejects entries missing a `post_url`.
- Ensure `follower_count_at_scrape` is not null (required for the VC metric).

## E2E Tests (Playwright)

### Setup & Discovery
- Add 3 niche accounts.
- Simulate a webhook arrival.
- Verify the feed updates with a "Leaderboard" layout.
- Confirm the "Viral Multiplier" badge (e.g., "5.2x") appears on cards.

### User Notes Flow
- Click "Save to Playbook" on an outlier.
- Navigate to `/playbook`.
- Edit `hook_draft` field.
- Refresh page and verify persistence.

## Edge Cases

- **Private Accounts:** Apify returns no data. The UI should show "No public data found for @handle."
- **Niche Overlap:** Two users tracking the same account. The system should store separate `post` records per user (to maintain RLS isolation) or use a global `posts` table with a many-to-many `user_feeds` table. *Decision: v1 uses separate records for RLS simplicity.*
- **Zero Follower Accounts:** If a creator is just starting (0 followers), the Viral Coefficient is infinite. Cap the denominator at 1 for calculation purposes to avoid errors.

## Success Metrics
- **Data Latency:** Webhook to Feed update < 2 seconds.
- **Accuracy:** At least 90% of "Outliers" identified by the tool are confirmed by a manual check of the account's feed.
- **Cost Efficiency:** COGS per user remains < $0.50/mo.